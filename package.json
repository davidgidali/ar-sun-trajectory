{
  "name": "starmap-monorepo",
  "private": true,
  "scripts": {
    "dev": "npm run dev --workspace=@starmap/web",
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces"
  },
  "workspaces": [
    "packages/*"
  ]
}

